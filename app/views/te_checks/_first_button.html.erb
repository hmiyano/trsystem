<% if te_logged_in? %>

<!-- 1013:TEが自分のだけをチェックできるようにと思ったがこれじゃダメだった
  <% first = checklist.te_checks.find_by(trainee_id: @trainee.id) %>
-->

  <% if @trainee == current_trainee %>

    <% if current_trainee.first?(checklist) %>
      <%= form_tag(te_check_path(checklist.id), method: :delete) do %>
        <%= hidden_field_tag :checklist_id, checklist.id %>

        <%= hidden_field_tag :type, 'First' %>
        <%= submit_tag 'チェック済', class: 'btn btn-success btn-xs' %>
      <% end %>
    <% else %>
      <%= form_tag(te_checks_path) do %>
        <%= hidden_field_tag :checklist_id, checklist.id %>
        <%= hidden_field_tag :type, 'First' %>
        <%= submit_tag 'チェックする', class: 'btn btn-info btn-xs' %>
      <% end %>
    <% end %>
  <% else %>
        違う人のです
  <% end %>
  
<% elsif tr_logged_in? %>
  <% first = checklist.te_checks.find_by(trainee_id: @trainee.id) %>
  <% if first %>
    チェック済
    <%= checklist.te_checks.first.updated_at if checklist.te_checks.first %>
  <% else %>
    チェックされてない
  <% end %>
<% end %>
